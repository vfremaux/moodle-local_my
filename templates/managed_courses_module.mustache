

<!-- managed_courses_module -->
{{^hideifempty}}
<div class="block block_my_managed_courses">
    <div class="header">
        <div class="title">
            <h2>{{mymanagedcoursesstr}}</h2>
        </div>
    </div>

    <div id="mymanagedcourselist" class="content courselist">

        {{{buttons}}}

{{/hideifempty}}
        {{#hascourses}}

        {{{overviewedcourses}}}

        {{{simplecourses}}}

        {{#debuginfo}}
        <pre>
        {{debuginfo}}
        </pre>
        {{/debuginfo}}
        {{/hascourses}}

{{^hideifempty}}
        {{^hascourses}}
        {{{nocourses}}}
        {{/hascourses}}

    </div>
</div>
{{/hideifempty}}
<!-- /managed_courses_module -->
